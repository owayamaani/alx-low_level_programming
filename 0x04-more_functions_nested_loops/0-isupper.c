#include "main.h"

/**
 * _isupper - checks for characters that are uppercase
 * @c: character to be checked
 * Return: 1 if uppercase, 0 otherwise
 */
int _isupper(int c)
{
	return (c >= 65 && c <= 90);
}
