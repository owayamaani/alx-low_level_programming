#include <stdio.h>

/**
 * main - prints out with proper grammer,
 * but the outcome is a piece of art,
 * Return: 0 if well exited, non-zero if not
 */
int main(void)
{
		printf("with proper grammar, but the outcome is a piece of art,\n");
			return (0);
}
