#include <stdio.h>

/**
 * main - print all single digits of base 10
 * starting with 0 followed by a new line
 * Return: 0 always
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", n);
	printf("\n");
	return (0);
}
